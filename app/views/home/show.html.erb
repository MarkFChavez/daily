<% if user_signed_in? %>
  <p class="greeting"> Hi <%= current_user.email %> </p>
  <%= link_to "Logout", destroy_user_session_path, :method => :delete, :class => "logout" %>
<% else %>
  <%= link_to "Login", new_user_session_path %>
<% end %>
